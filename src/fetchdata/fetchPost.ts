import { db, firebase } from "../firebaseconfig/firebase"

interface post {

}

export const fetchPostnewPost = (): Promise<post> =>{
    return new Promise(async (resolve, reject) =>{
        
    })
}